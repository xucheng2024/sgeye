name: Update HDB Rental Data (Disabled)

on:
  workflow_dispatch: # Rental tables were removed; keep manual only (no schedule)

jobs:
  update-rental-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Disabled
        run: |
          echo "This workflow is intentionally disabled."
          echo "Rental tables were removed at the database layer (see supabase/migrations/drop_rental_tables.sql)."
          echo "No action taken."

